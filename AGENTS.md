# Mumu 평가 개발 지침

- 사용자와 한국어로 자연스럽고 간결하게 소통한다.
- 전체 목표와 남은 기능은 `docs/OPERATING_STATUS.md`를 먼저 확인한다. 현재 구현 범위로 완료의 정의를 축소하지 않는다.
- 운영 앱과 `/demo` 예시를 구분한다. 서버 성공 응답 없는 제출/저장/채점 완료 표시를 만들지 않는다.
- 학생 답안·평가 결과는 Neon Postgres가 기준이다. 운영 연결 오류를 localStorage·메모리 DB로 우회하지 않는다.
- 교사 API는 인증 + 승인 계정 + 소유권을 모두 검사한다. 학생 API는 평가 코드 + 해당 학생의 HttpOnly 참여 토큰으로 격리한다.
- AI 추천은 교사 확정이 아니다. 확정 전 결과를 학생에게 공개하지 않는다.
- 스키마는 `db/migrations`에 순서대로 추가한다. 이미 적용한 마이그레이션 수정 금지. 실제 학생 DB에 테스트 데이터를 넣지 않는다.
- `.env.local`, 토큰, DB 연결 문자열, 실제 학생 원문을 Git·로그·최종 답변에 노출하지 않는다.
- 변경 후 `npm test`, `npm run lint`, `npm run build:vercel`을 실행하고 검증 범위를 정확하게 보고한다. 단위/DB 테스트를 실제 브라우저 운영 검증으로 표현하지 않는다.
- Next.js 문서는 현재 설치 버전에 맞는 `node_modules/next/dist/docs/`의 해당 가이드를 우선 확인한다. 운영은 Next.js/Vercel이며 기존 Sites 예시는 보존한다.
